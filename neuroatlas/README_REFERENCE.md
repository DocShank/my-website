# NeuroAtlas protected reference

The exact first live mobile NeuroAtlas build is preserved at `first_live_original.html` and originated from commit `0434cece66b39b01d03ca93056ec5e817d12da82` (`Add mobile NeuroAtlas MRI learning prototype`).

Future development should treat that version as the visual/interaction baseline unless explicitly changed by the project owner.